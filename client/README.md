@"
# Expense Tracker — Frontend

React (Vite) + Bootstrap UI for the Expense Tracker app.

## Features
- List, add, edit, delete expenses
- Category badges, running total
- Responsive Bootstrap layout

## Local setup
1. Copy .env.example to .env and set VITE_API_URL
2. npm install
3. npm run dev
"@ | Out-File -FilePath client/README.md -Encoding utf8